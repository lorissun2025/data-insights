# 智能数据平台 - 客户部署指南

## 📦 给客户展示的几种方案

根据你的需求,我提供了以下几种部署方案,从简单到专业:

---

## 方案一: 云服务器部署 (推荐给客户) ⭐⭐⭐⭐⭐

### 优点
- ✅ 专业可靠,可7x24小时访问
- ✅ 独立域名,便于客户访问
- ✅ 可扩展性能好
- ✅ 数据安全可控

### 部署步骤

#### 1. 购买云服务器
推荐平台:
- **阿里云** (https://www.aliyun.com) - 国内推荐
- **腾讯云** (https://cloud.tencent.com) - 国内推荐
- **AWS** (https://aws.amazon.com) - 国外推荐
- **DigitalOcean** (https://www.digitalocean.com) - 简单便宜

配置建议:
- CPU: 2核或以上
- 内存: 4GB或以上
- 硬盘: 40GB SSD
- 系统: Ubuntu 20.04 / 22.04
- 带宽: 5Mbps或以上

预估成本: **¥100-300/月**

#### 2. 服务器初始化
```bash
# 连接到服务器
ssh root@your-server-ip

# 更新系统
apt update && apt upgrade -y

# 安装Docker
curl -fsSL https://get.docker.com | sh

# 安装Docker Compose
curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
chmod +x /usr/local/bin/docker-compose

# 验证安装
docker --version
docker-compose --version
```

#### 3. 上传项目代码
```bash
# 在本地打包项目
cd "/Users/sunsensen/claude code"
tar -czf 智能数据平台.tar.gz 智能数据平台/

# 上传到服务器 (替换your-server-ip)
scp 智能数据平台.tar.gz root@your-server-ip:/root/

# 或使用rsync (更高效)
rsync -avz "智能数据平台/" root@your-server-ip:/root/智能数据平台/
```

#### 4. 启动服务
```bash
# SSH连接到服务器后
cd /root/智能数据平台

# 启动所有服务
docker-compose up -d

# 查看服务状态
docker-compose ps

# 查看日志
docker-compose logs -f
```

#### 5. 配置域名 (可选)
```bash
# 安装Nginx
apt install nginx -y

# 配置域名 (假设你的域名是 data-insights.com)
nano /etc/nginx/sites-available/data-insights

# 添加以下配置:
server {
    listen 80;
    server_name data-insights.com www.data-insights.com;

    location / {
        proxy_pass http://localhost:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}

# 启用配置
ln -s /etc/nginx/sites-available/data-insights /etc/nginx/sites-enabled/
nginx -t
systemctl restart nginx
```

#### 6. 配置SSL证书 (HTTPS,推荐)
```bash
# 安装Certbot
apt install certbot python3-certbot-nginx -y

# 自动配置SSL
certbot --nginx -d data-insights.com -d www.data-insights.com

# 自动续期
certbot renew --dry-run
```

#### 7. 客户访问地址
```
http://your-server-ip
或
http://data-insights.com (如果配置了域名)
https://data-insights.com (如果配置了SSL)
```

#### 8. 管理命令
```bash
# 停止服务
docker-compose down

# 重启服务
docker-compose restart

# 查看日志
docker-compose logs -f main-service

# 更新代码后重新部署
git pull  # 如果使用git
docker-compose down
docker-compose up -d --build
```

---

## 方案二: 本地演示 (临时展示) ⭐⭐⭐

### 优点
- ✅ 快速启动,无需配置
- ✅ 适合当面演示
- ✅ 成本为零

### 缺点
- ❌ 需要你的电脑在场
- ❌ 客户无法独立访问
- ❌ 不适合长期使用

### 操作步骤

#### 方法1: 本地启动
```bash
cd "/Users/sunsensen/claude code/智能数据平台"
./start-all.sh

# 打开浏览器
open frontend/login.html
```

#### 方法2: 局域网共享 (让客户用手机访问)
```bash
# 1. 启动服务
./start-all.sh

# 2. 查看你的IP地址
ifconfig | grep "inet " | grep -v 127.0.0.1

# 假设你的IP是 192.168.1.100

# 3. 让客户的手机连接同一WiFi
# 4. 客户浏览器访问: http://192.168.1.100:8000
```

#### 方法3: 使用ngrok (临时公网访问) ⭐
```bash
# 1. 安装ngrok
brew install ngrok  # Mac
# 或从 https://ngrok.com 下载

# 2. 注册ngrok账号并获取authtoken
ngrok authtoken your-token-here

# 3. 启动服务
./start-all.sh

# 4. 新开终端,创建临时公网地址
ngrok http 8000

# 5. 会看到类似这样的输出:
# Forwarding https://abc123.ngrok.io -> http://localhost:8000

# 6. 把 https://abc123.ngrok.io 发给客户
# 客户在任何地方都能访问! (有效期8小时免费版)
```

---

## 方案三: Vercel/Netlify 部署 (仅前端) ⭐⭐⭐⭐

### 优点
- ✅ 免费托管
- ✅ 全球CDN加速
- ✅ 自动HTTPS
- ✅ 部署简单

### 缺点
- ❌ 只能部署前端
- ❌ 需要单独部署后端API

### 步骤

#### 1. 准备前端代码
```bash
cd "/Users/sunsensen/claude code/智能数据平台/frontend"

# 创建vercel.json配置
cat > vercel.json << EOF
{
  "version": 2,
  "routes": [
    {"src": "/(.*)", "dest": "/$1"}
  ]
}
EOF
```

#### 2. 部署到Vercel
```bash
# 安装Vercel CLI
npm i -g vercel

# 登录并部署
vercel

# 按提示操作:
# - 登录Vercel账号
# - 项目名称: data-insights
# - 目录: ./frontend
# - 设置: 默认即可

# 部署完成会得到一个URL:
# https://data-insights.vercel.app
```

#### 3. 配置后端API地址
由于API在不同的服务器,需要修改前端API地址:

在所有HTML文件中,找到:
```javascript
const API_BASE_URL = 'http://localhost:8000';
```

改为你的后端API地址:
```javascript
const API_BASE_URL = 'https://your-backend-api.com';
```

---

## 方案四: GitHub Pages (免费演示) ⭐⭐⭐

### 步骤

#### 1. 创建GitHub仓库并推送代码
```bash
cd "/Users/sunsensen/claude code"
git init
git add 智能数据平台/
git commit -m "Initial commit"

# 创建GitHub仓库后
git remote add origin https://github.com/your-username/data-insights.git
git push -u origin main
```

#### 2. 启用GitHub Pages
- 访问 https://github.com/your-username/data-insights/settings/pages
- Source选择: Deploy from a branch
- Branch选择: main, / (root)
- 点击Save

#### 3. 访问地址
```
https://your-username.github.io/data-insights/frontend/login.html
```

---

## 🎯 推荐方案对比

| 方案 | 成本 | 难度 | 专业度 | 适用场景 |
|------|------|------|--------|----------|
| **云服务器+Docker** | ¥100-300/月 | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 正式商用,长期使用 |
| **本地演示** | 免费 | ⭐ | ⭐⭐ | 面对面演示 |
| **ngrok临时访问** | 免费 | ⭐ | ⭐⭐⭐ | 临时给客户展示 |
| **Vercel前端** | 免费 | ⭐⭐ | ⭐⭐⭐⭐ | 快速展示前端 |
| **GitHub Pages** | 免费 | ⭐⭐ | ⭐⭐⭐ | 开源展示 |

---

## 🚀 快速开始给客户展示 (最简单)

### 5分钟快速方案 (使用ngrok)

```bash
# 1. 启动服务
cd "/Users/sunsensen/claude code/智能数据平台"
./start-all.sh

# 2. 安装ngrok (如果没有)
brew install ngrok

# 3. 创建临时公网地址
ngrok http 8000

# 4. 复制显示的https地址发给客户
# 例如: https://abc123.ngrok.io

# 完成! 客户现在可以访问了
```

### 专业方案 (云服务器,推荐)

```bash
# 1. 购买云服务器 (阿里云/腾讯云)
# 2. 上传代码
scp -r "智能数据平台/" root@your-server-ip:/root/

# 3. SSH连接服务器
ssh root@your-server-ip

# 4. 启动服务
cd 智能数据平台
docker-compose up -d

# 5. 把服务器IP发给客户
# 完成!
```

---

## 📝 给客户的访问说明

创建一个简单的说明文档发给客户:

```
========================================
  智能数据平台 - 访问指南
========================================

📍 访问地址: http://your-server-ip

🔑 登录账号:
   用户名: admin
   密码: admin123

📱 推荐浏览器:
   - Chrome (推荐)
   - Safari
   - Firefox
   - Edge

💡 使用提示:
   1. 首次访问可能需要几秒加载
   2. 如遇问题,请刷新页面
   3. 推荐使用1920x1080或更高分辨率
   4. 移动设备也可正常访问

📞 技术支持:
   如有问题请联系: xxx@xxx.com
========================================
```

---

## 🔒 安全建议

1. **修改默认密码**
   - 在backend/app.py中修改admin/admin123

2. **启用HTTPS**
   - 使用Let's Encrypt免费SSL证书

3. **配置防火墙**
   ```bash
   ufw allow 80/tcp
   ufw allow 443/tcp
   ufw enable
   ```

4. **定期备份**
   ```bash
   # 备份数据
   tar -czf backup-$(date +%Y%m%d).tar.gz 智能数据平台/
   ```

---

## 需要我帮你做什么?

如果你要立即给客户展示,我可以帮你:

1. ✅ **生成ngrok临时地址** (5分钟搞定)
2. ✅ **准备部署到云服务器的完整包** (专业方案)
3. ✅ **创建演示视频或截图**
4. ✅ **编写客户使用手册**

请告诉我你需要哪种方案? 🚀
